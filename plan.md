# RRL 재고 모니터링 서비스 개발 계획 (plan.md)

## 1. 프로젝트 개요

이 프로젝트의 1차 목표는 **RRL(더블알엘) 제품 재고 확인 서비스**를 만드는 것이다.  
사용자가 원하는 쇼핑몰, 제품, 사이즈, 확인 시간대, 확인 주기를 설정하면 시스템이 이를 바탕으로 주기적으로 재고를 확인하고, 조건에 맞는 결과가 나오면 **메일과 Telegram Bot으로 알림을 전송**한다.

현재는 임시로 **회사 Proxmox 서버**에 올려서 사용 중이며, 이후에는 **개인 AWS EC2**로 이전할 예정이다.  
장기적으로는 **개인 Mac PC 환경에서도 운영 가능하도록 구조를 정리**할 계획이다.

초기 지원 대상 사이트는 아래 두 곳이다.

- Cultizm
- The Hyundai

향후 아래 사이트들도 순차적으로 추가할 예정이다.

- Ralph Lauren 공식 홈페이지
- Farfetch

---

## 2. 핵심 목표

### 2.1 사용자 관점 목표
- 원하는 사이트의 원하는 제품을 자동으로 추적할 수 있어야 한다.
- 원하는 사이즈 기준으로 재고를 감지할 수 있어야 한다.
- 사용자가 지정한 시간대와 주기 기준으로만 확인할 수 있어야 한다.
- 재고 발견 시 즉시 알림을 받을 수 있어야 한다.
- 알림 방식은 사용자가 직접 선택할 수 있어야 한다.
- 단순 품절과 시스템 에러를 구분해서 알려줘야 한다.

### 2.2 개발 관점 목표
- 사이트 추가가 쉬운 구조여야 한다.
- 크롤러 실패 원인을 빠르게 파악할 수 있어야 한다.
- 불필요한 코드와 중복 로직을 줄여 유지보수성을 높여야 한다.
- AWS EC2 배포 및 운영에 적합한 구조여야 한다.
- 추후 로컬(Mac) 환경에서도 쉽게 실행 가능해야 한다.

---

## 3. 1차 버전 기능 요구사항

### 3.1 모니터링 설정
사용자는 최소한 아래 항목을 설정할 수 있어야 한다.

- 사이트명
- 제품명 또는 제품 식별 정보
- 제품 URL 또는 검색 키워드
- 원하는 사이즈
- 재고 확인 시작 시간
- 재고 확인 종료 시간
- 확인 주기(예: 5분, 10분, 30분)

예시:

- 사이트: Cultizm
- 제품: RRL denim jacket
- 사이즈: M
- 확인 시간: 09:00 ~ 23:00
- 확인 주기: 10분

---

### 3.2 재고 발견 알림
재고가 확인되면 아래 채널로 알림을 전송한다.

- Email
- Telegram Bot

알림에는 최소한 아래 정보가 포함되어야 한다.

- 사이트명
- 제품명
- 사이즈
- 재고 발견 시각
- 제품 URL
- 확인 결과 요약

---

### 3.3 알림 정책
사용자가 알림 정책을 선택할 수 있어야 한다.

#### 정책 A: 24시간 1회 알림
- 특정 제품/사이즈 조합에 대해 재고를 발견하면 1회 알림 전송
- 이후 24시간 동안 동일 조건 알림 중복 전송 방지

#### 정책 B: 확인 응답 전까지 반복 알림
- 재고 발견 시 최초 알림 전송
- 사용자가 확인 응답을 할 때까지 일정 간격으로 반복 알림 전송
- 반복 주기 역시 설정 가능하도록 설계하는 것이 좋다

예:
- 최초 감지 시 즉시 알림
- 이후 10분마다 재알림
- 사용자가 Telegram에서 확인 응답 시 중단

---

### 3.4 오류 감지 및 오류 알림
다음 두 상황은 반드시 구분해야 한다.

#### 정상 케이스
- 검색 결과가 없어서 제품을 찾지 못함
- 제품은 찾았지만 원하는 사이즈 재고가 없음

#### 오류 케이스
- 검색창 탐지 실패
- 검색 필드 접근 실패
- 버튼/DOM 셀렉터 변경으로 검색 자체 수행 불가
- 페이지 로딩 실패
- 로그인/봇 차단/예상치 못한 예외 발생

즉,  
**“검색을 했는데 결과가 없음”** 과  
**“검색 시도 자체를 하지 못함”** 은 다른 상태로 처리해야 한다.

오류 케이스는 운영상 중요한 이슈이므로 별도로 알림을 보내야 한다.

오류 알림 예시:
- `[ERROR] Cultizm 검색창 탐지 실패`
- `[ERROR] The Hyundai 상품 검색 DOM 변경 가능성`
- `[ERROR] Farfetch 페이지 응답 비정상`

---

## 4. 시스템 동작 흐름

### 4.1 기본 흐름
1. 사용자가 모니터링 조건을 등록
2. 스케줄러가 등록된 작업을 주기적으로 실행
3. 사이트별 크롤러가 검색 또는 상품 페이지 확인
4. 결과를 상태 코드로 정규화
5. 재고 발견 시 알림 정책에 따라 통지
6. 오류 발생 시 운영용 오류 알림 전송
7. 로그 및 마지막 확인 상태 저장

---

## 5. 상태 정의 제안

사이트별 결과를 공통 상태로 정리해두면 이후 확장이 쉬워진다.

### 5.1 추천 상태값
- `IN_STOCK`: 원하는 사이즈 재고 있음
- `OUT_OF_STOCK`: 제품은 확인되었으나 재고 없음
- `PRODUCT_NOT_FOUND`: 검색 결과에 제품이 없음
- `SEARCH_FAILED`: 검색창/검색 필드를 찾지 못해 검색 자체 실패
- `PAGE_ERROR`: 페이지 응답 이상 또는 파싱 실패
- `BLOCKED`: 봇 차단/접근 제한 가능성
- `UNKNOWN_ERROR`: 기타 예외

이 상태 체계를 먼저 정해두면,
- 알림 정책
- 로그 저장
- UI 표시
- 에러 대응
을 일관되게 처리할 수 있다.

---

## 6. 권장 아키텍처

초기 버전부터 사이트별 구현을 분리하는 구조가 좋다.

### 6.1 권장 디렉터리 예시

```text
project/
├─ app/
│  ├─ main.py
│  ├─ scheduler/
│  │  └─ jobs.py
│  ├─ crawlers/
│  │  ├─ base.py
│  │  ├─ cultizm.py
│  │  ├─ thehyundai.py
│  │  ├─ ralphlauren.py
│  │  └─ farfetch.py
│  ├─ services/
│  │  ├─ stock_checker.py
│  │  ├─ notifier_email.py
│  │  ├─ notifier_telegram.py
│  │  └─ alert_policy.py
│  ├─ repositories/
│  │  ├─ monitor_repository.py
│  │  ├─ result_repository.py
│  │  └─ notification_repository.py
│  ├─ models/
│  │  └─ monitor.py
│  ├─ utils/
│  │  ├─ logger.py
│  │  ├─ time_utils.py
│  │  └─ retry.py
│  └─ config/
│     └─ settings.py
├─ tests/
├─ requirements.txt
├─ .env
└─ plan.md
```

---

## 7. 사이트 크롤러 구조 제안

사이트마다 HTML 구조가 다르므로 공통 인터페이스를 두는 것이 좋다.

### 7.1 공통 인터페이스 예시
각 사이트 크롤러는 최소 아래 메서드를 구현하도록 한다.

- `search_product()`
- `fetch_product_page()`
- `check_stock()`
- `normalize_result()`

또는 더 단순하게 아래 하나로 통합할 수도 있다.

- `check(monitor_config) -> CheckResult`

### 7.2 공통 반환 객체 예시
각 크롤러가 아래와 같은 공통 결과 객체를 반환하도록 정리하면 이후 로직이 매우 단순해진다.

```python
{
    "status": "IN_STOCK",
    "site": "cultizm",
    "product_name": "RRL Denim Jacket",
    "size": "M",
    "url": "https://...",
    "checked_at": "2026-03-15T14:00:00+09:00",
    "message": "size M available",
    "raw": {}
}
```

---

## 8. 성능 개선 방향

실제 코드를 보기 전 기준으로, 우선 아래 항목을 중점 점검하면 된다.

### 8.1 중복 요청 제거
- 같은 상품 URL을 너무 자주 중복 조회하고 있지 않은지 확인
- 같은 페이지를 여러 번 파싱하고 있지 않은지 확인
- 검색 후 상품 상세 페이지 진입 구조라면 검색 결과 캐싱 가능성 검토

### 8.2 불필요한 브라우저 자동화 최소화
- Selenium/Playwright를 무조건 띄우는 구조라면 비용이 크다
- 가능한 경우 requests + HTML 파싱으로 처리
- JS 렌더링이 꼭 필요한 사이트만 브라우저 기반으로 분리

### 8.3 체크 작업 병렬화
- 여러 모니터링 작업이 있을 경우 순차 처리만 하면 지연이 누적될 수 있다
- 사이트별 rate limit를 고려하면서 작업 큐나 제한된 병렬 처리 적용
- 단, 초기 버전에서는 무리한 병렬화보다 안정성 우선

### 8.4 타임아웃/재시도 일관화
- 요청 타임아웃 값 통일
- 일시적 네트워크 오류에 대한 재시도 정책 분리
- 영구 오류와 일시 오류를 구분

### 8.5 파싱 로직 안정화
- CSS selector 또는 XPath 하드코딩이 많다면 상수화 필요
- 셀렉터 변경 탐지 로그 강화
- 사이트별 파싱 실패 이유를 상세 로그로 남기기

---

## 9. 불필요한 부분 정리 기준

기존 소스를 받을 때 아래 기준으로 정리하면 된다.

### 9.1 제거 대상 후보
- 더 이상 사용하지 않는 util 함수
- 미사용 import
- 테스트용 하드코딩
- 죽은 코드(dead code)
- 사용되지 않는 환경변수
- 중복 notifier 구현
- 중복 예외 처리
- 의미 없는 wrapper 함수

### 9.2 통합 대상 후보
- 사이트별로 거의 동일한 HTTP 요청 로직
- 유사한 알림 메시지 생성 코드
- 중복된 시간 조건 체크 로직
- 중복된 결과 저장 로직
- 중복된 logging 포맷

---

## 10. 저장해야 할 데이터 제안

운영을 위해 최소 아래 정보는 저장하는 것이 좋다.

### 10.1 모니터링 설정
- monitor_id
- site
- product keyword 또는 product_url
- target size
- check_start_time
- check_end_time
- check_interval_minutes
- notification_policy
- repeat_interval_minutes
- enabled 여부

### 10.2 최근 확인 결과
- last_status
- last_checked_at
- last_success_at
- last_error_at
- last_message
- product_url
- screenshot_path 또는 raw_html_path(선택)

### 10.3 알림 이력
- last_notified_at
- notification_count
- acknowledged_at
- deduplication_key

이 정보가 있어야
- 24시간 1회 알림
- 응답 전 반복 알림
- 오류 재발 추적
이 가능해진다.

---

## 11. 알림 설계 포인트

### 11.1 중복 알림 방지
아래 기준으로 dedup 키를 두는 것이 좋다.

```text
site + product_identifier + size + status
```

이 키를 기준으로 최근 알림 시각을 관리하면 중복 발송을 제어할 수 있다.

### 11.2 Telegram 응답 처리
반복 알림 정책을 쓰려면 Telegram에서 다음 액션이 가능해야 한다.

- 확인 완료
- 다시 알림 유지
- 모니터링 끄기(선택)

초기 버전에서는 우선 간단하게 아래 정도면 충분하다.

- `/ack <monitor_id>`
- 또는 inline button 기반 확인 처리

---

## 12. 배포 계획

### 12.1 현재
- 회사 Proxmox 서버에 임시 배포

### 12.2 다음 단계
- 개인 AWS EC2 이전
- systemd 또는 Docker 기반 실행
- 환경변수(.env)로 민감정보 관리
- CloudWatch 또는 파일 로그 기반 운영

### 12.3 장기 계획
- 개인 Mac PC에서 실행 가능한 구조 유지
- 운영환경 의존성이 적은 방식으로 정리
- 크론 + Python 프로세스 또는 Docker Compose 기반 고려

---

## 13. AWS EC2 기준 운영 체크리스트

- Python 런타임 버전 고정
- Telegram Bot token 환경변수 분리
- Email SMTP 설정 외부화
- 타임존 명확화(기본 UTC 저장, 표시만 KST 권장)
- 로그 파일 rotation
- 프로세스 자동 재시작 설정
- 장애 시 운영용 Telegram 알림 전송
- robots.txt 및 사이트 정책 확인
- 과도한 요청 방지를 위한 throttling

---

## 14. 리스크 및 주의사항

### 14.1 사이트 구조 변경
가장 빈번한 장애 원인이다.  
따라서 “재고 없음”보다 “검색 실패/DOM 변경 의심”을 정확히 분리해야 한다.

### 14.2 봇 차단
Ralph Lauren 공식몰, Farfetch 같은 사이트는 추후 차단 가능성이 더 높을 수 있다.  
초기 구조부터 아래를 고려해야 한다.

- User-Agent 관리
- 요청 빈도 제한
- 브라우저 자동화 필요 여부 분리
- 로그인/쿠키 의존 여부 검토

### 14.3 알림 폭주
반복 알림 기능은 편리하지만 잘못 설계하면 알림 폭탄이 될 수 있다.  
반드시 아래 제한이 필요하다.

- 최소 반복 주기 제한
- 최대 발송 횟수 제한
- 사용자 ack 처리
- 오류 알림도 rate limit 필요

---

## 15. Codex와 함께 진행할 작업 범위

이 `plan.md`와 기존 소스코드를 함께 기준으로 아래 작업을 순서대로 진행하면 된다.

### 1단계: 현재 코드 분석
- 실행 흐름 파악
- 사이트별 크롤링 방식 파악
- 알림 구조 파악
- 설정값 관리 방식 파악

### 2단계: 정리 작업
- 미사용 코드 제거
- 중복 로직 제거
- 설정 구조 정리
- 로그 포맷 정리

### 3단계: 구조 개선
- crawler interface 정리
- 공통 result model 정리
- notifier 분리
- alert policy 분리

### 4단계: 운영 개선
- 예외 처리 체계화
- 오류 알림 강화
- AWS EC2 배포 구조 정리
- 환경변수/보안 설정 정리

### 5단계: 확장 준비
- Ralph Lauren / Farfetch 추가 가능한 구조로 리팩터링
- 사이트 추가 시 수정 범위를 최소화

---

## 16. Codex에 바로 줄 수 있는 작업 지시문 예시

```text
이 plan.md를 기준으로 현재 소스코드를 분석하고 아래 작업을 수행한다.

1. 현재 코드의 실행 흐름을 설명한다.
2. 미사용 코드, 중복 코드, 불필요한 import를 식별한다.
3. 사이트별 크롤러 구조를 공통 인터페이스 기반으로 리팩터링한다.
4. 재고 결과 상태를 공통 status enum으로 정리한다.
5. 알림 정책(24시간 1회 / ack 전 반복)을 분리된 로직으로 정리한다.
6. 검색 실패와 단순 품절을 구분하는 에러 처리 구조를 만든다.
7. AWS EC2 배포를 고려해 설정 파일과 환경변수 구조를 정리한다.
8. 변경 사항은 파일별 diff 관점으로 설명한다.
```

---

## 17. 이번 단계의 산출물 목표

이번 단계에서는 아래 수준까지 정리되면 충분하다.

- 서비스 목적과 범위 명확화
- 1차 기능 요구사항 정의
- 오류와 정상 상태 구분 기준 정의
- 향후 사이트 확장을 위한 구조 방향 제시
- 기존 소스 리뷰 및 리팩터링 작업을 Codex에 넘길 수 있는 기준 문서 확보

---

## 18. 다음 단계

다음으로는 기존 소스코드를 기준으로 실제 리뷰를 진행한다.

우선순위는 아래 순서를 권장한다.

1. 현재 코드 구조 분석
2. 미사용/불필요 코드 제거
3. 상태값 및 에러 처리 정리
4. 알림 정책 분리
5. AWS EC2 배포 구조 정리
6. 신규 사이트 확장 가능 구조로 리팩터링
